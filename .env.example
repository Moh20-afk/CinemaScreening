# Optional: search TMDB for posters and synopses when a cinema feed has no artwork.
TMDB_API_KEY=

# Protect GET /api/cron/listings. Vercel Cron sends Authorization: Bearer $CRON_SECRET.
CRON_SECRET=

# Later: authentication and cloud persistence
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
